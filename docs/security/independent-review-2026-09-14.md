# Independent security review — 2026-09-14

> This review describes the exact September 14 revisions below, not the
> current app. The dated tracker records later dispositions. See
> [security notes](README.md) for current scope and reporting policy.

| | |
|---|---|
| Wallet revision | `winnowwallet/winnow` `main` @ `aae3b28` (merge of #96, 2026-09-14) |
| Census revision | `winnowwallet/census` `main` @ `248fa1f` (2026-09-14) |
| Reviewer | An agent-driven review engaged by the project owner on fresh clones. **Not a human third party and not the uninvolved auditor the gate report names as open follow-up.** The reviewer had no part in writing the code, which is the material difference from the August 2026 same-project passes. |
| Method | Read-only. No production edits, no commits, no pushes. Every finding below was traced in source by the reviewer personally after a first pass by parallel read-only sub-reviews; file:line citations are to the revisions above. Severity uses the table in `audit-manifest.md`. |
| Scope | The ~13,300 lines that landed after the August audit (recovery, people, shared savings, receive labels, census refresh, Tor bridge), the two runtime trust inputs that did not exist then (census download, sender lookup), the census repository as the upstream of the bundled peer list, and second-opinion spot checks on header validation, keys, CI, and the Rust bridge. `SEC-001`–`SEC-028` were **not** re-verified. |

## Summary

**One High, seven Medium, twenty-two Low, three informational. Nothing Critical: no path to fund loss without a prior eclipse, and no path to a secret.**

The August work held up where it was aimed. Every signing and broadcasting path still passes a binding review of the exact transaction; device authentication fails closed; the Keychain item is add-only, device-bound and group-less; the persisted descriptor is neutered everywhere; the storage root is backup-excluded; the E2E surface is compiled out of Release. Those are recorded under *Verified sound* with citations so the next reader knows they were looked at.

What the August passes did not have in front of them is where the findings cluster:

1. **The peer-consensus rule does not deliver the mitigation the gate report relies on (IR-001, High).** The accepted single-peer-eclipse risk was dispositioned on the strength of "cross-source agreement". The cross-source *set* is built, but the vote is a strict majority unweighted by source, one source class may hold two of three seats, the outvoted peer is banned permanently and struck from the persisted good-peers file, and a wrongly pinned filter header is never re-verified — every later honest answer throws. Two agreeing peers of one class therefore hide payments for the window they control and leave the wallet unable to sync with anyone afterwards, manual honest nodes included, until re-import. This is a design defect independent of who the attacker is; the census work (IR-003, IR-004) is simply the cheapest way to become that attacker.
2. **The census list is an unauthenticated central trust input (IR-003, IR-004, IR-005, Medium/Low).** In Tor mode it is the origin of every automatic peer. It is unsigned, unpinned, published by a workflow token onto an unprotected branch, deployable without validation on a direct push, and imported at release time by hashing whatever bytes arrived. Runtime exposure is bounded — refresh is one manual button, expiry falls back to the bundle — and the publisher/wallet validation really is shared. Signing the document and pinning the generator to a commit closes most of this.
3. **A mid-period checkpoint plus no timestamp rule makes fabricated confirmations free under eclipse at first sync (IR-002, Medium).** Moving the checkpoint to a retarget boundary closes it at no cost.
4. **The new stores are validated but not authenticated (IR-010, Medium).** A substituted signer key in `people.json` becomes an invisible co-owner of shared savings; the register's own hostile-persistence model rated the vault-store analogue High when a semantic check could catch it — here only authenticity can.
5. **Resource bounds on the peer connection are count-bounded where they need to be byte-bounded, and one is not bounded at all (IR-025, IR-026, IR-027, Medium).** The wire framer never releases consumed bytes — reproduced here at +257 MiB resident after 256 MiB of valid frames — so any peer, or a long honest filter scan, can grow one connection past jetsam; the message backlog caps messages, not bytes, so 256 unsolicited 4 MB blocks park a gigabyte per seat; and the seat-0 peer can freeze header sync forever by answering "no headers", silently and unpenalised. None of the nine fuzz targets or the sanitizer runs can see any of these, which is why they survived the August evidence.
6. **Hardening in the post-audit code (IR-011 – IR-024, IR-028 – IR-033, Low/Info).** The vault paths sit outside the `spending` gate; a post-broadcast commit failure is swallowed; card import skips the key-network check the builder performs; names and cards are unbounded; the seed-bearing export staging file is weaker than the Keychain item it copies and is never swept; `redactedPreview` fails open; several stores have no pinned protection class.

Governance and evidence (IR-007, IR-008, IR-009): the audit manifest describes a tree that no longer exists, the egress inventory is three entries short, and the written gate still says NO-GO while mainnet is the shipped default with no recorded decision. None of these is a vulnerability; all of them are the kind of drift the register's own rules exist to prevent, and a reader of `docs/security/` today is misled by them.

**What this review is not.** It is a read of the source at one revision by an agent the owner engaged, with parallel sub-reviews whose every conclusion was re-traced by the reviewer before entering this file. No test was written, no mutation was run, no device was touched, and `SEC-001`–`SEC-028` were taken as closed. The gate report's outstanding item — an uninvolved human auditor — remains outstanding.

## Findings

IDs follow discovery order, not severity; the Summary above lists them by severity.

| ID | Severity | Invariant | Summary | Evidence |
|---|---|---|---|---|
| IR-001 | **High** | S5 | **Two peers of one source class outvote and permanently ban the third, and the filter state they pin can never be displaced by honest peers.** `DiversityPolicy.admits` is a ceiling: with `peerCount` 3 one class may hold 2 seats (`PeerDiversity.swift:110-118`). `FilterSync` adopts a *strict majority* of cfcheckpt and cfheaders answers (`FilterSync.swift:371-409`, `:546-554`) and calls `pool.misbehaving` on the minority, which disconnects, bans for the session, and strikes the endpoint from the persisted good-peers file (`PeerPool.swift:219-226`). Replacements dialled mid-scan are deliberately never consulted (`FilterSync.swift:252-258`, `:272-279`), so after the eviction the sync runs on the two agreeing peers, which also serve the cfilters (`:280-283`), and the headers they pin are persisted (`:284-288`). Once pinned, every later cfcheckpt (`checkPinnedBoundaries`, `:415-424`) and every batch anchor (`anchorPreviousHeader`, `:607-611`) **throws** on a disagreeing answer rather than re-verifying; nothing in `Sources/WinnowApp` handles `checkpointMismatch`/`filterHeaderMismatch`, and `filters.json` is removed only on wallet adopt or delete (`AppModel.swift:1163`, `:1319`); `rollBack` prunes only above a header-chain fork (`FilterSync.swift:806-817`). Consequence: an attacker who seats 2 of the 3 automatic peers for one sync (a) scans that window with filters that hide the victim's payments, and (b) leaves the wallet unable to sync with *any* honest peer afterwards — three honest manual nodes included — until the wallet is re-imported. This defeats the mitigation named for the accepted single-peer-eclipse risk (`gate-report.md:109-124`, "cross-source agreement"): the cross-source *set* is built (`FilterSync.swift:458-473`) but the vote is not weighted by source, so both agreeing peers may be the same class. In Tor mode both the onion and clearnet automatic lists are census-derived `.fallback` (`PeerPool.swift:716-727`; DNS is disabled unless `.direct`, `SeedResolver.swift:57`). Not Critical: headers stay PoW-checked and blocks merkle-verified, so this cannot fabricate confirmations or reach a secret. | Traced as cited. Evidence gap noted by the reviewer: `FilterSyncAdversaryTests.swift:42,193-195` states that "two liars would agree and form a majority for the lie" but no test exercises two same-class liars against one honest cross-class peer, and no test exercises recovery from a wrongly pinned filter header. **Fix direction:** (1) treat a majority drawn from a single source class as *no majority* — do not ban, do not persist; (2) on a pinned-vs-announced mismatch, drop pinned headers back to the last checkpoint that was corroborated across classes and re-verify instead of throwing; (3) make `misbehaving` from a filter vote session-scoped rather than striking the persisted file, or require cross-class corroboration before striking. |
| IR-002 | Medium | S5 | **A mid-period checkpoint leaves the first retarget unchecked, and no timestamp rule exists, so an attacker who eclipses a wallet at first sync can extend the chain at difficulty 1.** `expectedBits` returns `nil` when the period-first header is unavailable and `requireDifficulty` then skips the check (`HeaderChain.swift:232-257`). The mainnet checkpoint is at 900,000 (`NetworkParams.swift:222-230`); 900,000 mod 2016 = 864, so the adjustment at 901,152 has `firstIndex = −864` and is unchecked — documented at `HeaderChain.swift:56-59`. Timestamps are not validated at all (`:60`). An eclipsing peer may therefore serve a header at 901,152 with `bits = powLimit` (any target ≤ powLimit is accepted, `:263-274`) and hold difficulty 1 thereafter by spacing timestamps exactly 600 s (elapsed == timespan → unchanged). A fabricated block then costs 2^32 hashes — seconds on one GPU — against ~2^78 for an honest one, so under eclipse the attacker can show any number of confirmations for any transaction at no hash cost. Precondition that keeps this Medium: the wallet must hold no honest work past 901,152 (fresh install, or a rebuilt header file); a chain already carrying honest work cannot be displaced, because a replacement branch must carry strictly more work (`:363-367`). | Retarget arithmetic otherwise matches Core exactly (period-first at h−2016, previous at h−1, clamp [ts/4, ts×4], powLimit cap, `:248-257`). `scripts/refresh-checkpoint` and `CheckpointGenerator.swift` accept any height; nothing enforces a boundary. **Fix:** ship the checkpoint at a height ≡ 0 (mod 2016) — `firstIndex` then resolves to the checkpoint header itself and the gap closes for free — and enforce `height % 2016 == 0` in the generator. Separately consider Core's future-drift rule (time ≤ now + 2 h) so timestamp lies cannot ease difficulty 4× per period. |
| IR-003 | Medium | S5, S12 | **The census peer list is an unauthenticated, centrally published trust input with three unprotected write paths, and its release-time import is self-attesting.** The list is not signed; the wallet trusts TLS alone with no certificate pinning (`NetworkRoute.swift:90` — the only delegate is `RedirectPolicy`; no `serverTrust` handling anywhere in `Sources/`), so an MDM-installed root or a mis-issued certificate suffices. Write paths: the daily census job pushes to `main` with the workflow token under `contents: write` (`census/.github/workflows/peer-census.yml:38-39,167`), so `main` cannot be branch-protected against it; a direct push to `census/**` deploys through `site.yml` with **no** validation (`--validate-peer-list` runs only inside `peer-census.yml:150`); and the Cloudflare token publishes arbitrary content. At wallet release, `scripts/generate-fallback-peers` fetches the live URL over plain `RoutedHTTPClient(route: .direct)` with no pinned commit and no signature (`FallbackPeerGenerator.swift:26,117-121`); the `// Source SHA256` line hashes whatever bytes arrived (`:103-110`) and is never compared to the census repository's committed blob; `scripts/check-release-policy:11-29` checks only dates and the format of that line. The resulting ~3,000-line diff of IP literals and onion strings is not reviewable by eye. Combined with IR-001, this is the deterministic way to seat the two deciding peers. | Runtime exposure is bounded: the refresh is manual only (`SettingsView.swift:105` → `AppModel.swift:2445-2466`), replaces the bundle entirely, applies only on mainnet, expires after 7 days and falls back to the bundle (`CensusCatalog.swift:46-56`, `PeerPool.swift:711-715`), and invalid downloads never touch the pool. Shared publisher/wallet validation is real (`census/Package.swift` pins the wallet at `096427c`; `PeerList.swift:4,40`). **Fix:** sign `peers.json` (e.g. minisign/ed25519 with the public key compiled into the wallet and the generator); pin the generator to a census commit and compare the SHA256 to that blob; protect `main` in the census repo and give the daily job a scoped deploy key; validate on every deploy, not only in the census job. |
| IR-004 | Medium | S5 | **Tor Sybil with no infrastructure compromise.** The census selects the 2,000 lowest-latency at-tip onions (`census/Sources/WinnowCensus/PeerList.swift:26-38`; on 2026-09-13, 3,177 were cut to 2,000) and the netblock rule applies to clearnet only, so one host running many hidden services listed on btcnodes.io takes a proportional share of the bundled Tor list; the dial race then favours fast responders (acknowledged at `PeerDiversity.swift:5-8`). With patience and bandwidth alone an operator can hold the two deciding seats of IR-001 for a meaningful fraction of Tor-mode launches. | Distinct from IR-003 because it needs neither Cloudflare, the repository, nor a certificate. **Fix:** cap onions per census run by an operator-independence heuristic where possible (shared guard/introduction fingerprints are not observable, so the honest answer is a per-class quota on *deciding* votes — see IR-001 fix 1), and randomise rather than latency-sort the onion selection. |
| IR-005 | Low | S5, S10 | `CensusCatalog.validated` accepts an overlay with zero entries (`CensusCatalog.swift:65-79`; the publisher refuses this but a MITM bypasses the publisher), which empties the automatic pool; `RedirectPolicy` blocks HTTPS→HTTP but allows cross-host HTTPS redirects (`NetworkRoute.swift:57-66`). Degradation only. | **Fix:** require a minimum entry count per overlay; refuse redirects off the census host. |
| IR-006 | Low | S1, S3 | **`BIP39.seed` normalises with NFD, not the NFKD the spec requires.** `BIP39.swift:58-59` uses `decomposedStringWithCanonicalMapping`; BIP39 mandates `decomposedStringWithCompatibilityMapping`. No passphrase surface exists in `Sources/` today and English words are ASCII, so nothing shipped is affected. The day a passphrase field ships, a passphrase containing a compatibility character (ligature, full-width digit, superscript) derives a seed no other wallet can reproduce — recovery elsewhere fails silently. | Demonstrated on this machine: passphrase `ﬁnance１` → NFD `ef ac 81 6e 61 6e 63 65 ef bc 91`, NFKD `66 69 6e 61 6e 63 65 31`; the 12-word English mnemonic is identical under both. **Fix:** one token, plus a vector with a compatibility character in the passphrase. |
| IR-007 | Low | S12 | **The audit manifest and invariant matrix describe a tree that no longer exists.** `audit-manifest.md` lists `Sources/BitcoinP2P`, `Sources/BitcoinCore`, `Sources/WalletCore/SilentPayments`, `Internal/Serialization.swift`, `WalletCore.swift`, `VaultDetailView.swift`, `VaultSignView.swift` — none exist on `main`; `findings.md` SEC-017 cites `.github/workflows/security.yml`, which is now `fuzz.yml`. The egress inventory (`audit-manifest.md:94-109`) lists three points; the current tree adds the census refresh (`CensusCatalog.swift:24` via `RoutedHTTPClient`), the opt-in Esplora `/tx/{txid}` fetch (`SenderLookup.swift:31-32`), and the Arti client's own directory and guard traffic. The S11 row still describes the removed tweak-index client. | The register's own rule (`docs/security/README.md:15-18`) is that evidence must stay tied to revisions; a reader following these pointers today cannot reproduce them. **Fix:** re-freeze the manifest at the current revision and regenerate the egress inventory with the grep it prescribes. |
| IR-008 | Low | S12 | **The written mainnet gate still says NO-GO, but mainnet is the shipped default.** `gate-report.md:7` "Decision: NO-GO … does not authorize a mainnet release", last revised 2026-08-23. `AppModel.swift:1796` `defaultNetwork = .mainnet`; the README says "Mainnet by default"; the App Store link landed 2026-09-12 (`8b64f4d`). The report's own rule is that "the mainnet release decision belongs to the project owner, on evidence" — the decision was evidently made, but no GO row, date, or basis is recorded, so the security folder now contradicts the product. | **Fix:** add the decision, its date, and what it rested on to the gate report — or, if the decision was to accept the open items, say which. |
| IR-009 | Info | S12 | `SECURITY.md` routes reports to a public GitHub issue, while `findings.md:10-12` says exploit detail belongs in a private advisory. Deliberate (`09fb6db`, `e449f72`), but GitHub private vulnerability reporting costs nothing and matches the register's stated policy. | — |

| IR-010 | Medium | S3, S8 | **`people.json` is validated but not authenticated, and a substituted signer key is invisible in every screen that shows a shared-savings co-owner.** The store is semantically validated on load (`PeopleStore.swift:330-396`) and written atomically under `completeFileProtectionUntilFirstUserAuthentication` (`:87`), the same model as `VaultStore.validate` — but neither carries authenticity. Address substitution is caught by the user because the review screen renders the derived address next to the name (`SendView.swift:286-289`). Signer-key substitution is not: the creation screen lists names only (`SharedSavingsViews.swift:79`), the account detail co-owners line lists names only (`AccountDetailView.swift:80`), and `recomputeSharedSavings` labels co-owners by matching derived keys *against the tampered record itself* (`AppModel.swift:1903-1921`). `SEC-009`/`SEC-019`/`SEC-020` can catch a harmful *shape*; nothing can catch a valid-but-foreign key except authenticity. Precondition: write access to the app container (the directory is excluded from backup), which is the hostile-persistence model the register already rates against. | Traced as cited. **Fix:** HMAC the people and vault stores under a Keychain-held secret, and render at least a fingerprint of each co-owner key on the creation and detail screens. |
| IR-011 | Low | S2 | **Vault and MuSig2 sign/broadcast paths run outside the `spending` exclusion gate that `SEC-008` introduced.** Only three sites hold it (`AppModel.swift:1656,1706,1755`); `partialSignVaultSpend` (`:2169`), `finalizeAndBroadcastVaultSpend` (`:2185`) and `MuSig2SignView.aggregateAndBroadcast` (`:490-540`) do not. Every one of them still re-reviews the exact PSBT before signing or finalizing (`Vault.swift:712,842,883,923,936`), so this is not an authorization bypass; the exposure is a concurrent self-conflicting broadcast from the same vault. | **Fix:** claim the same gate (or a per-vault gate) before the first await on each path. |
| IR-012 | Low | S9 | **A vault spend's commit failure after broadcast is swallowed and the caller discards the result.** `recordVaultSpend` wraps the store call in `try? … ?? false` (`AppModel.swift:2208-2210`); `finalizeAndBroadcastVaultSpend` discards it (`:2194`) and the session reports "Sent" regardless (`VaultSpendSession.swift:200-202`). If `persist()` or `validate` fails at that instant the spent coin stays in `record.utxos`, the next spend can reselect it, and both transactions signal RBF (`TransactionBuilder.swift:51`), so the second can replace the first. Contrast the single-sig path, which `try await`s `wallet.commit` (`:1719`). `recordSpend` legitimately returns `false` for an already-recorded spend (`VaultStore.swift:320`), so the fix is to distinguish a throw from `false`, not to make `false` fatal. | Traced as cited. |
| IR-013 | Low | S3, S6 | **A shared-savings card is not checked for the network of its extended keys.** `SharedSavingsCard.decode` compares only the card's `network` field (`People.swift:271-272`) and hands the descriptor to `Vault.init`, whose `requireSupportedShape` binds `.extended(key, _)` without comparing the key's HD prefix to the wallet network (`Vault.swift:147-151,157-161`). The builder path does check (`People.swift:101`, `VaultSetup.swift:37`). A hand-crafted card carrying `tpub` keys and `"network":"mainnet"` is therefore filed as a mainnet vault. Not reachable with Winnow-generated cards. | **Fix:** apply the same `hdNetwork(for:)` comparison in `Vault.init` so the boundary refuses what the builder refuses. |
| IR-014 | Low | S11 | **The custom explorer accepts plain `http://`, and an explorer-supplied "funding address" can become a saved pay-to.** `explorerBaseURL` admits `http` (`AppModel.swift:2534`), so the opt-in `/tx/{txid}` fetch (`SenderLookup.swift:31-32`) can travel in cleartext, leaking txid and IP and letting an on-path party forge the answer. The inferred address can then be tapped into the paste field (`RecipientsView.swift:171-175`) and saved as a person's destination (`:280-291`), later paid with `unverifiedFundingWarning` on review (`SendView.swift:326-331`). The consent dialog, two explicit taps, and the review warning are real controls; the design cannot verify that a funding address is a receive address at all. | **Fix:** require `https` for custom explorers; consider not offering "save as pay-to" from an inferred funding address. |
| IR-015 | Low | S8 | **`Vault.signers(of:)` counts unverified signature presence by pubkey** (`Vault.swift:298-316`), so a forged approval reply makes the progress line say "Approved by Alice" and enables Finish; `PSBT.finalize()` then verifies every signature against the recomputed sighash and refuses (`PSBTRoles.swift:470-537`). UI misrepresentation only. | **Fix:** verify partial signatures when counting approvals. |
| IR-016 | Low | S7 | **Card JSON size, `PersonRecord.name`, `VaultRecord.name`, and `destinationSource` are unbounded and unnormalised** (no cap in `WinnowCardCoding.decode`; contrast `ReceiveAddressLabelStore.normalizedLabel`, `:81-88`). A hostile card persists a multi-megabyte or multi-line name into `people.json`/`vaults.json`, re-decoded at every launch, and a multi-line name renders above the address on the review screen. | **Fix:** cap and normalise names at the card boundary the way receive labels already are. |
| IR-017 | Info | S4 | MuSig2 secret-nonce copies (`initialNonces`, `MuSig2SignView.swift:442`, and the `@State` value before `removeAll`, `:475,557`) are freed without zeroing; only the staged copy passed to `MuSig.partialSign` is zeroed (`MuSig2.swift:248`). In-memory only; never persisted or journaled. | — |

| IR-018 | Low | S1 | **The seed-bearing export staging file is protected more weakly than the Keychain item it copies, and nothing sweeps it after a crash.** `ExportStagingFile.protect` applies `completeUntilFirstUserAuthentication` (`ExportStagingFile.swift:57-62`) where the Keychain item is `WhenUnlockedThisDeviceOnly` (`KeychainStore.swift:25`). The file is removed on toggle, close, background, disappear, rewrite and `deinit` (`SettingsView.swift:411-413,457-474`; `ExportStagingFile.swift:15,22,41-45`), but not when the share completes, and `temporaryDirectory` appears nowhere else in `Sources/`, so a crash or SIGKILL with the export sheet open leaves the mnemonic in `tmp/` readable in the after-first-unlock state until iOS purges it. The class is asserted, not observed: `ExportStagingFileTests` checks only `isExcludedFromBackup`, and the `#if os(iOS)` block is unreachable from the package suite. On Mac (Designed for iPad, `project.yml:60-61`) no class is applied at all. | The path itself is otherwise well built: device authentication first (`AppModel.swift:1124-1127`), mnemonic loaded from the Keychain only for the call (`Wallet.swift:1571-1589`), preview redacted (`ImportBundle.swift:256-263`), backup-excluded directory. **Fix:** use `.complete`; sweep `tmp/winnow-export-*` at launch; give the `ShareLink` a generic `SharePreview` so Quick Look never thumbnails the JSON. |
| IR-019 | Low | S1 | **`ImportBundle.redactedPreview` fails open.** On any decode or re-encode failure it returns the original, unredacted JSON (`ImportBundle.swift:260,262`), which `CopyableTextBlock` then exposes with the cross-device interchange clipboard policy and a `ShareLink` (`SettingsView.swift:427`, `Components.swift:115-134`). Unreachable today because the input is the app's own encoder output, but it is the wrong default for a function whose only job is to keep a seed off the screen; it also redacts only the `mnemonic` key. | **Fix:** return a placeholder or throw on failure. |
| IR-020 | Low | S1 | **`wallet.json` and several other stores carry no explicit protection class.** `wallet.json` — the most privacy-sensitive non-secret file (xpub descriptor, full history, raw transactions) — is written with `.atomic` only (`Wallet.swift:1715,650`; `ImportBundle.swift:432`), as are `headers.bin`, `peers.json`, the rollback marker, the downloaded census and the Tor state directory; sibling stores pin `completeFileProtectionUntilFirstUserAuthentication` explicitly (`VaultStore.swift:44`, `PeopleStore.swift:87`, `ReceiveAddressLabelStore.swift:43`, `FilterSync.swift:838`, `TxBroadcaster.swift:832`). No `DataProtection` capability or entitlements file exists (`project.yml`), so the class depends on the platform default. | **Fix:** pin the class on every store, or declare the capability. |
| IR-021 | Low | S1 | **`UserDefaults` are backed up to iCloud/Finder and hold bounded privacy data:** the wallet master fingerprint (`backupPending.<fp>`), the user's own node `host:port`, the custom explorer URL, display name and Tor flag (`AppModel.swift:344-366,376`). No secret-shaped value. The excluded storage root protects everything else (`AppModel.swift:2614-2616`). | **Fix:** move endpoints and the fingerprint under the excluded root, or record as accepted. |
| IR-022 | Low | S1 | **No screen-recording or mirroring defence on the phrase reveal and backup screens.** `.privacySensitive()` is applied to both grids (`OnboardingView.swift:237`, `SettingsView.swift:564`) but bites only under `.redacted(.privacy)`, which is applied solely when inactive or backgrounded (`WinnowApp.swift:24-25`); nothing observes `UIScreen.isCaptured` / `capturedDidChangeNotification`. | **Fix:** hide the grid while the screen is captured. |
| IR-023 | Low | S1 | **The Keychain item carries no `kSecAttrAccessControl`;** the device-authentication gate is app-layer `LAContext` only (`KeychainStore.swift:25`, `AppModel.swift:12-23,2287-2290`), so any code running inside the process on an unlocked device reads the secret without a prompt. A documented design choice; recorded as defence in depth. | **Fix:** `SecAccessControlCreateWithFlags(.userPresence)` on the item. |
| IR-024 | Info | S1 | `WalletError.invalidBundle` echoes bundle-supplied `txid`/`scriptPubKey`/`network` strings into on-screen error text (`ImportBundle.swift:311,314,400,491`; `AppModel.swift:1059`) — not seed-shaped by schema, and `SEC-027` already closed the descriptor case. The seed-bearing import `TextEditor` has no `.privacySensitive()` (`OnboardingView.swift:294`) and the decoded bundle, mnemonic included, stays alive in the `importWallet` frame across the whole verification scan (`AppModel.swift:1057-1102`). Derived private keys (`Data`) are not scrubbed after signing (`Signer.swift:23-28`); only MuSig2 secret nonces are (`MuSig2.swift:247-248`). | Truncate echoed input; optional `.privacySensitive()`; best-effort `resetBytes` in a `defer` where the `Data` is uniquely referenced. |

| IR-025 | Medium | S10 | **`MessageFramer` never releases consumed bytes: a connection's memory grows with every byte it has ever received.** `nextMessage` consumes a frame with `buffer.removeFirst(total)` (`Framing.swift:67`). For `Data`, whose `SubSequence` is itself, that is a reslice that keeps the storage (the comment at `:46-47` notes the non-zero `startIndex`); the next `append` (`:39`) finds the storage uniquely referenced and grows it in place, so bytes before `startIndex` are never reclaimed. Reproduced on this machine with the struct verbatim: 256 MiB of 64 KiB `cfilter` frames → `buffered=0`, `startIndex=268,435,456`, **RSS +257 MiB**; 64 MiB of 32-byte `ping` frames → +31 MiB. Reachable by any connected peer streaming valid frames — a `ping` flood needs no state and is answered — and plausibly on the honest path: one connection serving a mainnet filter scan from the checkpoint carries on the order of a gigabyte of `cfilter`/`block` bytes in a single foreground session, past iOS jetsam limits. | Neither the `framing` fuzz target nor AddressSanitizer can see this: referenced memory is not a leak and round-trip semantics are unchanged. To reconcile: `findings.md:168,175,177` report resident size settling to 10–16 MB after a from-genesis signet sync with this same pool; those runs also record transport errors and a pool that never filled, and a replaced primary frees its framer — a hypothesis, not a measurement. Closed issue #2 ("~580 MB on mainnet at launch") was attributed to the sync burst; this retention is a candidate contributor. **Discriminating test:** log `bufferedCount`/`startIndex` on the primary during a soak. **Fix:** keep the O(1) reslice in `nextMessage` but compact once per receive — after the drain loop in `receiveLoop` (`PeerConnection.swift:572-574`), or reset to `Data()` when empty. Per-frame copying inside `nextMessage` is the wrong fix: O(remaining) per frame, so a 4 MiB chunk of ~131k `ping`s would memmove ~270 GB. |
| IR-026 | Medium | S10 | **Per-connection backlog and subscriber buffers are bounded by message count, not bytes.** `backlog` keeps the newest 256 decoded messages (`PeerConnection.swift:164,628-629`) and each `events()` stream the newest 256 (`:475`). Nothing this client sends solicits `block`, `tx` or unknown-command messages outside a pending request, yet all are decoded and retained: 256 × 4 MB `.unknown` (any 12-byte command with a valid checksum) or 256 valid `block`s park ~1 GiB per peer in `backlog`, plus a copy per live subscriber, plus IR-025's retention of the same bytes; three seats → ~3 GiB. Precondition: a connected peer. | **Fix:** refuse to backlog what can never answer a request this client makes (`.unknown`, unsolicited `.block`, `.tx` with no window or collector waiting, `.addr`), and add a byte budget for the rest. |
| IR-027 | Medium | S5, S10 | **The seat-0 peer can freeze header sync indefinitely without ever being penalised.** `PeerPool.syncHeaders` asks only `peers.first` (`PeerPool.swift:381`). A primary that answers every `getheaders` with an empty `headers` returns cleanly (`HeaderChain.swift:393`), earns `transportSucceeded` (`PeerPool.swift:474`) and is never evicted; one replaying a single real stale sibling is absorbed four times (`HeaderChain.swift:426-435`), each an O(n) prefix copy, then also returns cleanly — `SyncOutcome.staleSiblings` is consumed only by a test. `catchUp` (`:497-504`) consults another peer only when that peer's **handshake-time** `peerStartHeight` exceeds our tip by more than 100, a value never refreshed; unsolicited BIP130 `headers` from other seats are purged before every request (`PeerConnection.swift:418`) and never otherwise consumed. So the chain — and `FilterSync`, which scans only to `chain.height` (`FilterSync.swift:242-245`) — stops advancing until churn seats a fresh peer claiming >100 blocks (~17 h) ahead, silently: no error, no eviction, the wallet simply shows no new payments. Amplifier: the seated attacker is promoted to `knownGood`, persisted, and dialled before fallbacks at the next launch (`PeerPool.swift:575-578,703-710`). Precondition: winning seat 0, which the dial race hands to the fastest responder (`PeerDiversity.swift:5-8`). | Distinct from the accepted single-peer eclipse: this is one peer in an otherwise honest pool of three. Recovery today is user-driven only ("Reset and shuffle peers"). **Fix:** treat an empty reply from a peer whose claimed height is above our tip as a data fault, or rotate the primary; consume `staleSiblings`; refresh height claims from BIP130 announcements. |
| IR-028 | Low | S10 | `MempoolWindow`'s `notfound` handler runs `seenOrder.firstIndex(of:)` for every vector whether or not the txid was seen (`MempoolWindow.swift:239-245`): a 50,000-vector `notfound` costs ~5×10⁸ `Data` compares, 10–25 s of one core, repeatable while a window is open and pinning the actor so `.tx` matching is delayed. Bounded per message and by the 20-minute window; the payment is still credited by the confirmed scan. | **Fix:** search `seenOrder` only when `seenTxids.remove(hash) != nil` (also `:185,266`), or index by position. |
| IR-029 | Low | S10 | `TxBroadcaster.serve` re-serves per duplicate vector and emits unbounded events (`TxBroadcaster.swift:787-809`): no dedupe within a `getdata`, and the `.served` state it writes is never consulted, so one 50,000-vector `getdata` naming a pending txid makes the phone send the transaction 50,000 times and enqueue 100,000 events into every unbounded `events()` subscriber (`:223`). Precondition: a pending broadcast. | **Fix:** dedupe per message; refuse to re-serve a txid already served to that peer. |
| IR-030 | Low | S7, S10, S12 | **Fuzz coverage omits every post-audit parser, and two register statements are stale.** The harness's nine targets (`Exercise.swift:6-16`) drive 19 wire commands but not `addr`/`getaddr`; no target covers `CensusCatalog.decode/validated`, `DNSJSON.hosts`, `EsploraSenderLookup.parseFundingAddresses`, `FundingSources` (a hand-written push parser fed from peer-served blocks), `Taproot.ControlBlock(serialized:)`, `PersonPaste`/`WinnowCardCoding`, `PersistedPeers.decode`, `PeopleStore.decodePayload` or `ReceiveAddressLabelStore.readLabels`. `findings.md:136` and `invariant-matrix.md:44` say Winnow handles no `addr` message; it has decoded `addr` since `6dcf100` (`Messages.swift:390-400`). The message is only buffered and dropped, never cached, so the *cap* conclusion still holds; the statement does not. | Manual bounds review of each unfuzzed parser found no reachable trap (see *Hostile input — verified sound*). **Fix:** add targets for the list above; correct the two rows. |
| IR-031 | Low | S10 | **Four app-owned stores read the whole file before any size check** — the `SEC-012` pattern not applied: `PeopleStore.swift:109`, `PeerPool.swift:150` (and no count cap on load; the writer caps at 100), `VaultStore.swift:63`, `Wallet.swift:664`. `ReceiveAddressLabelStore`, `CensusCatalogStore`, `HeaderChain`, `FilterSync` and `TxBroadcaster` measure first. Same threat model and fix as `SEC-012`. | — |
| IR-032 | Low | S10 | `SeedResolver.dohGET` (`SeedResolver.swift:121-137`) uses an uncapped `session.data(for:)`; only the routed path (128 KiB cap, `:50-60`) is wired in production (`PeerPool.swift:147`). Dead code that would be a live gap if ever re-wired. | **Fix:** delete it, or route it through `RoutedHTTPClient`. |
| IR-033 | Low | S10 | **No read-idle deadline or pong check on peer connections**, and no idle timeout on a Tor stream. `receiveLoop` (`PeerConnection.swift:554-580`) waits indefinitely; the keepalive sends `ping` every 60 s but never checks for `pong` (`:603-604,664-677`), so a trickling or silent peer keeps its seat until the next request against it times out — already listed as "slowloris … remaining" in `invariant-matrix.md:44`. In the bridge, `copy_bidirectional` (`lib.rs:116`) has no idle timeout, so a remote that never closes holds one of the 64 permits until Arti drops the circuit, and permit exhaustion is silent (`:77`). | **Fix:** a read-idle deadline tied to the ping interval; an idle timeout around the copy. |

### Hostile input — verified sound

- Every wire count cap precedes allocation: payload ≤ 4,000,000 checked before `total` and before the `subdata` (`Framing.swift:59-62`); headers ≤ 2,000 (`Messages.swift:415-417`); inv/getdata/notfound ≤ 50,000 (`Inventory.swift:64-66`); cfheaders ≤ 2,000, cfcheckpt ≤ 100,000 (`Messages.swift:199,264`); addr ≤ 1,000 (`:393-395`); block transactions ≤ 100,000 (`Block.swift:126-128`); transaction inputs/outputs bounded by remaining bytes, witness items ≤ 1,000, var-data ≤ 4,000,000 (`Transaction.swift:125,143,155,162,173,175`); GCS filter n ≤ 1,000,000 with the minimum-bits check before any loop (`GCSFilter.swift:73-77,95-98`). A `headers` entry's `txn_count` is read as a minimal varint and must be 0 (`Messages.swift:420-421`, `Wire.swift:74-92`).
- `CensusCatalog`: 4 MiB before `JSONDecoder`; exact key set; per-overlay counts; host ≤ 255 bytes; every fixed index (`bytes[32..34]`, IPv4/IPv6 octets) sits behind a length guard in the same short-circuiting `guard` or comes from a fixed-size `in_addr`/`in6_addr`; `decodeBase32` shifts a `UInt32` with ≤ 12 pending bits; `OnionChecksum.hash` always pads to at least input + 1.
- `SenderLookup`: 2 MiB re-checked after fetch; root must be an object with `vin` ≤ 10,000; every address re-validated by the fuzzed `AddressDecoder`. `FundingSources.pushedItems` checks `endIndex − index ≥ count` before every `subdata`; `Taproot.ControlBlock` enforces `33 + 32m, m ≤ 128`; the P256K x-only tweak-add *throws* on an off-curve x rather than trapping and is consumed with `try?`.
- The trap grep (`fatalError | precondition( | try! | as! | .first! | .last! | [0]`) over the network, funding, people, label, lookup and app sources found only constructor invariants on lengths the decoder already fixed, a Release stub, and indexes behind real guards — e.g. `FilterSync.swift:374` reads `checkpoints[0]` when `count ≤ 1`, and `collectedCheckpoints` throws on an empty list before it is reached (`:346-349`). No trap reachable from peer, file, HTTP or pasted bytes was found.
- Low-difficulty header chains cannot consume memory: every staged header must carry `bits == expected` and real work before the next is read (`HeaderChain.swift:248-257,355-358`); a no-progress batch is a replay, at most four per sync (`:397-401,440`), and any other data fault evicts the peer.
- `MempoolWindow`: `seenTxids` ≤ 10,000 FIFO, `inFlight` ≤ 512, 30 s expiry (`MempoolWindow.swift:178-187,288-296`). `TxBroadcaster`: pending ≤ 4,096, raw tx ≤ 4 MB, file ≤ 64 MiB, attempts ≤ 63, schedule ≤ 366 d (`TxBroadcaster.swift:162-168,490-527`).
- Tor bridge SOCKS handshake: `Buffer::new_precise()` is 1,024 bytes and `recv.buf()` returns only the deficit slice, so the socket cannot over-read and `note_received`'s panic precondition is unreachable (tor-socksproto 0.46.0).

### Secret containment — verified sound

- The Keychain item is add-only (`SecItemAdd`, throws on duplicate rather than overwriting), `WhenUnlockedThisDeviceOnly`, `kSecAttrSynchronizable = false`, in the app's own default access group — no entitlements file, no app groups, no `SecItemUpdate` anywhere, no weaker accessibility constant anywhere (`KeychainStore.swift:22-29,51-58`). Observed, not merely read, by `AppTests/KeychainAttributeTests.swift:55-57`.
- The persisted wallet descriptor is built from the *neutered* account key on every path (`Wallet.swift:645,675,680-686`; `ImportBundle.swift:465`), so `wallet.json` and `AppModel.walletDescriptor` never carry an xprv; the master key is derived per call and never held in state (`Wallet.swift:1525-1545`, `AppModel.swift:2272-2282`).
- Import stores the secret only via `keyStore.store` (`ImportBundle.swift:459`); view state holding the pasted JSON is cleared immediately after commit and on close/background/disappear (`OnboardingView.swift:385,408-416`). The bundle format carries the mnemonic only as an explicit opt-in on the encoder side (`ImportBundle.swift:276`, `Wallet.swift:1588-1589`).
- The storage root `Application Support/BTCSwift/<network>/` has `isExcludedFromBackup` set every time it is resolved, and the app refuses storage if that fails (`AppModel.swift:2604-2621`). An iCloud or Finder restore to another device yields neither seed nor history.
- Every clipboard write of wallet material goes through `ClipboardPolicy` (`Components.swift:40-58,128,167`; `ReceiveView.swift:64`); the only direct `UIPasteboard.general.string =` is inside the DEBUG-only E2E fixture (`AppModel.swift:417`). Neither phrase grid has `.textSelection`, so the system long-press copy cannot reach the seed.
- No `print`, `os_log`, `Logger`, `NSLog`, `debugPrint` or `dump(` in `Sources/`; the Rust bridge has no `tracing` subscriber or `log` sink, so Arti's internal events are dropped and proxy errors are discarded (`lib.rs:81`).
- The explorer/census HTTP client is an ephemeral `URLSession` with `urlCache = nil`, no cookies and `.reloadIgnoringLocalCacheData` (`NetworkRoute.swift:76-80`), so no `Cache.db` residue of txid or address lookups. The bundled-papers `WKWebView` is file-only with JavaScript off and every non-file scheme blocked by a content rule (`Components.swift:198-240`).
- `SensitivePresentationEpoch` is a late-result token, not a screen control: an auth or Keychain result that completes after cancel or background can enter view state only if the token is still current *and* the scene is not backgrounded (`SensitivePresentationEpoch.swift:9-29`). Sound for its stated purpose.
- `E2EMode.current` is a compile-time `nil` in Release (`E2EMode.swift:263-297`), so the auth bypass and the E2E Keychain wipe are unreachable in shipped builds.
- Provenance note: PR #94's merge commit `096427c` is a no-op merge (its second parent is already an ancestor of the first; the diff is empty). The routed-Tor, validated-refresh and people-destination code the roadmap attributes to "recovery" landed via `1e924d7` on 2026-09-13.

### Money path — verified sound

- Every signing or broadcasting path has a binding review: single-sig send and RBF go through `SendPreview.authorizes` (`AppModel.swift:1716,1761`); every vault path re-runs `reviewSpend` on the exact PSBT being signed or finalized (`Vault.swift:712,842,883,923,936`); `PSBT.combined` refuses a different unsigned transaction (`PSBTRoles.swift:345-374`), so a pasted reply cannot swap outputs silently. `authorizes` remains exhaustive (`AppModel.swift:1557-1593`).
- A saved recipient's address is re-decoded against the *current* network at review time (`People.swift:140-147` → `AddressDecoder.scriptPubKey(for:network:)`), the people file is per-network, and the review screen shows the derived address, not only the name.
- `SEC-009`/`SEC-019`/`SEC-020` run on both shared-savings creation paths (`createSharedSavings` and `addSharedSavings` both reach `VaultStore.add` → `Vault.init`); change ownership is derived from descriptor scripts and local coordinates, never from PSBT metadata (`Vault.swift:486-523`, `AppModel.swift:2135-2140`).
- Device authentication fails closed: `canEvaluatePolicy == false` throws `deviceAuthUnavailable` (`AppModel.swift:16-18`); the E2E bypass requires `E2EMode.current`, which is a compile-time `nil` in Release (`E2EMode.swift:263-272`). Sends, bumps, wallet creation, seed import/export/reveal, destroy, and every `withMasterKey` are gated.
- Entropy and randomness: `SystemRandomNumberGenerator` for wallet entropy (`Wallet.swift:632-635,705-708`), MuSig2 nonces (`MuSig2.swift:438-441`, BIP327 masking at `:104-108`) and Schnorr aux-rand (`Signer.swift:62-65`).
- Sender inference has one call site, reachable only through a confirmation dialog after an explicit tap (`RecipientsView.swift:158-163,214-221,247-259`); no background invocation.

## Verified sound

Recorded so that a later reader knows what was looked at and found correct, not only what was found wrong.

- **Retarget arithmetic** matches Core's `pow.cpp`: period-first at h−2016 and previous at h−1 (the 2015-interval span), clamp, powLimit cap, and a wrapping multiply that cannot overflow because powLimit × 4 × timespan < 2^256 (`HeaderChain.swift:232-257`, `UInt256.swift:120-132`). `UInt256.target(compact:)` mirrors `SetCompact` including the negative and overflow rules and does not trap for any 32-bit input (`UInt256.swift:172-185`).
- **Fork choice:** equal work is refused; both the append and the replace path PoW-check and difficulty-check every header (`HeaderChain.swift:316-376`).
- **CI:** every action is SHA-pinned; triggers are `pull_request`, never `pull_request_target`; the self-hosted jobs are gated on a same-repository head (`ci.yml:59`, `node-tests.yml:74,150`); signing secrets are step-scoped in the release and App Store lanes.
- **Tor bridge** (`TorBridge/src/lib.rs`): loopback-only listener bound to port 0, non-loopback peers dropped, 64-stream permit cap, CONNECT only, `.i2p` refused, 30 s handshake and 120 s connect timeouts, an isolated Tor client per stream. Two notes rather than findings: iOS has no per-app loopback isolation, so any foregrounded app can use the SOCKS proxy while Winnow runs (it proxies; it does not expose wallet data); `panic = "abort"` in `Cargo.toml` means any Rust panic terminates the app.
- **Tor-mode DNS:** `SeedResolver.routed` returns nothing unless the route is `.direct` (`SeedResolver.swift:57`), so seed hostnames never leave via system DNS in Tor mode.
- **ATS:** only `NSAllowsLocalNetworking` (for the loopback proxy), no arbitrary loads; no `UIBackgroundModes` (`project.yml:26-27,83`).
- **Census refresh runtime hygiene:** manual trigger only, 4 MiB cap enforced on `Content-Length` and while streaming, ephemeral session with no cookies or cache, invalid or expired documents never reach the pool.

## Validation record

| Date | Source | Check | Result |
|---|---|---|---|
| 2026-09-14 | `aae3b28` | `HeaderChain.requireDifficulty` vs Core `GetNextWorkRequired` by hand | Period span and clamp identical; checkpoint 900,000 not on a boundary, so the 901,152 adjustment is unchecked (IR-002) |
| 2026-09-14 | `aae3b28` | Foundation NFD vs NFKD over a passphrase with U+FB01 and U+FF11 | Byte sequences differ; English mnemonic identical (IR-006) |
| 2026-09-14 | `aae3b28` | `grep` of every `checkpointMismatch`/`filterHeaderMismatch` consumer in `Sources/WinnowApp` | None; `filters.json` removed only at `AppModel.swift:1163,1319` (IR-001 stickiness) |
| 2026-09-14 | `aae3b28` | `grep` of every `refreshPeerCatalog` / `replace(with:)` caller | One UI button (`SettingsView.swift:105`); no timer or launch hook (bounds IR-003) |
| 2026-09-14 | `aae3b28` | Existence of the paths named in `audit-manifest.md` | Seven missing; `security.yml` missing (IR-007) |
| 2026-09-14 | `aae3b28` | `MessageFramer` copied verbatim into a standalone probe; 256 MiB of 64 KiB `cfilter` frames, then 64 MiB of 32-byte `ping` frames | `buffered=0`, `startIndex=268,435,456`, RSS +257 MiB; ping flood +31 MiB (IR-025) |
| 2026-09-14 | `aae3b28` | `grep` of `exclusively(` call sites | Three (`AppModel.swift:1656,1706,1755`); vault sign/broadcast paths absent (IR-011) |
| 2026-09-14 | `aae3b28` | `git diff 44320ad 096427c` | Empty — PR #94's merge is a no-op; the "recovery" code landed via `1e924d7` |

## Tracker

Filed 2026-09-14 as one public issue per finding, label `security`, at the owner's direction.

**Status, 2026-09-14** — one branch, winnow#137, carries the fixes; census#15 the census side. Fixed there: IR-001, IR-002, IR-003, IR-004, IR-005, IR-007, IR-010 through IR-020, IR-022, IR-024 through IR-033. Closed without a change: IR-006 and IR-019 (fixed upstream before the review's branch landed), IR-008 (the owner recorded GO). Recorded as accepted: IR-021 (`SEC-029`). Owner-gated and still open: IR-009 (`SECURITY.md` now points at private vulnerability reporting, which the repository must enable) and IR-023 (an access-control class on the Keychain item is a device-behaviour decision). The census signing mechanism of IR-003 ships with an empty trusted-key list; the owner's three steps are in `docs/census-signing.md`.

**Status, 2026-09-15** — IR-023 fixed (winnow#120): the wallet's Keychain item now carries `SecAccessControlCreateWithFlags(.userPresence)` over `WhenUnlockedThisDeviceOnly`; the `LAContext` the app's own check evaluates is handed to the read that follows, so the user is asked once; an item stored by 0.7.1 or earlier gets the access control in place on its next open. `AppTests/KeychainAttributeTests` reads the recorded constraint back and exercises the in-place upgrade; the simulator records the constraint but does not enforce it, so enforcement stays with the device-evidence item. Owner-gated and still open: IR-009.

**Status, 2026-09-18** — the owner chose public GitHub issues for all bug and vulnerability reports. This supersedes the earlier private-reporting recommendation and owner-setup item for IR-009. [SECURITY.md](../../SECURITY.md) is the current reporting policy; the dated review observations above are retained as history.

**Census activation, 2026-09-18** — the publisher signing secret and pinned
public key are configured in [census#18](https://github.com/winnowwallet/census/pull/18).
The wallet now requires signed refreshes and caches, rejects an empty trust
configuration, and loads signatures on every census generator path. Bundled
peers are regenerated from a signed pinned commit. This supersedes the
empty-key setup note above; [the signing guide](../census-signing.md) records
the key and rotation procedure.

**Bundled-list removal, 2026-09-18** — the app no longer ships a peer snapshot,
and its peer generator/crawler and snapshot release gate are removed. Signed
manual downloads and verified caches remain; fresh installations can use DNS
or manual peers. This supersedes the bundled-generation part of the activation
note, not its signature requirement.

| ID | Issue | ID | Issue | ID | Issue |
|---|---|---|---|---|---|
| IR-001 | winnow#98 | IR-012 | winnow#109 | IR-023 | winnow#120 |
| IR-002 | winnow#99 | IR-013 | winnow#110 | IR-024 | winnow#121 |
| IR-003 | winnow#100, census#13 | IR-014 | winnow#111 | IR-025 | winnow#122 |
| IR-004 | winnow#101, census#14 | IR-015 | winnow#112 | IR-026 | winnow#123 |
| IR-005 | winnow#102 | IR-016 | winnow#113 | IR-027 | winnow#124 |
| IR-006 | winnow#103 (closed: fixed upstream in `f8b5639` before the review's PR landed) | IR-017 | winnow#114 | IR-028 | winnow#125 |
| IR-007 | winnow#104 | IR-018 | winnow#115 | IR-029 | winnow#126 |
| IR-008 | winnow#105 (closed: GO recorded by the owner, 2026-09-14) | IR-019 | winnow#116 (closed: the preview was removed upstream in `f8b5639`) | IR-030 | winnow#127 |
| IR-009 | winnow#106 | IR-020 | winnow#117 | IR-031 | winnow#128 |
| IR-010 | winnow#107 | IR-021 | winnow#118 | IR-032 | winnow#129 |
| IR-011 | winnow#108 | IR-022 | winnow#119 | IR-033 | winnow#130 |

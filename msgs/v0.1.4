v0.1.4 — TestFlight cloud signing

One fix: the release pipeline signs with Apple's cloud-managed distribution
certificate instead of minting disposable certificates that outlive their
ephemeral runners.

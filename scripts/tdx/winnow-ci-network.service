[Unit]
Description=Network isolation for Winnow test QEMU user
Before=winnow-controller.service
[Service]
Type=oneshot
ExecStartPre=-/usr/sbin/nft delete table inet winnow_ci
ExecStart=/usr/sbin/nft -f /etc/winnow-ci.nft
RemainAfterExit=yes
[Install]
WantedBy=multi-user.target

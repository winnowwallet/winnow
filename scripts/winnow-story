#!/bin/bash
# Run app-owned story tooling with all app paths anchored to this checkout.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."
export WINNOW_STORY_REPOSITORY="$PWD"
exec swift run --package-path "$PWD/Tools/Story" --quiet winnow-story "$@"
